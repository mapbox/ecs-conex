version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.11
    steps:
      - setup_remote_docker:
        docker_layer_caching:
      - checkout
      - run: docker info >/dev/null 2>&1 || service docker start
      - run: npm install
      - run: npm test
